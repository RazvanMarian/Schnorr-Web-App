<MudDrawer @bind-Open="@Open" Elevation="1">
    <MudDrawerHeader>
        <img src="logo.png" alt="logo" style="max-width: 4rem; max-height: 4rem"/>
        <MudText Typo="Typo.h6">Schnorr Sign</MudText>
    </MudDrawerHeader>

    <MudNavMenu Class="mud-width-full">
        <MudDivider Class="my-2"/>
        <AuthorizeView>
            <NotAuthorized>
                <MudNavLink Href="/" Icon="@Icons.Filled.Dashboard">Dashboard</MudNavLink>
            </NotAuthorized>

            <Authorized>
                <MudNavLink Href="/" Icon="@Icons.Filled.Dashboard">Dashboard</MudNavLink>
                <MudNavLink Href="/Profile" Icon="@Icons.Filled.AccountCircle">My profile</MudNavLink>
                <MudNavLink Href="/Generate-Key" Icon="@Icons.Filled.VpnKey">Generate key</MudNavLink>
                <MudNavLink Href="/User-Keys" Icon="@Icons.Filled.Lock">My keys</MudNavLink>
                <MudNavLink Href="/Sign-Document" Icon="@Icons.Filled.DocumentScanner">
                    Sign a document
                </MudNavLink>
            </Authorized>
        </AuthorizeView>
    </MudNavMenu>
</MudDrawer>

@code{

    [Parameter]
    public bool Open { get; set; } = true;

}